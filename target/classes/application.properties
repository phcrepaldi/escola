# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/Escola?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=

# Connection Configuration

# JPA Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Database Initialization Configuration
spring.jpa.hibernate.ddl-auto=update

#Server Cofiguration
server.error.whitelabel.enabled=false
server.error.include-message=always
server.error.include-exception=true
server.error.include-stacktrace=always


#none           - n�o vamos inicializar nenhuma BD
#create         - cria o banco de dados toda vez que a aplica��o � iniciada (elimina a BD no arranque e depois cria a BD - sem tabelas e sem dados)
#create-drop    - cria e dropa o banco de dados toda vez que a aplica��o � iniciada (cria a BD mas sem dados, e quando desligam ele elimina a BD e os dados
#validade       - valida o esquema do banco de dados, faz as altera��es necess�rias, mas n�o dropa o banco de dados (verifica se houve altera��o nas nossas classes, se houve n�o arranca a aplica��o
#update         - atualiza o esquema do banco de dados (atualiza a BD com as altera��es nas nossas classes)
